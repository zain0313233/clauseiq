[build]
# Set Railway variable RAILPACK_INSTALL_CMD=npm install (fixes Linux lockfile vs npm ci).
buildCommand = "npx prisma generate && npm run build"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
